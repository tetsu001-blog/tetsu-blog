# ブログ運用・開発ルール

## 画像処理スクリプト（fix_images.py）の自動実行ルール

ユーザーから以下のような自然言語による指示やリクエストがあった場合、AIは自動的に `python fix_images.py` を実行してください。

### 実行トリガー（発話例）
- 「画像パスを整えて」「画像パスを修正して」
- 「画像を修正して」「画像変換して」
- 「Obsidianの画像をHugo用に変換して」
- 「画像のリンクチェック・最適化をして」
- 「fix_imagesを実行して」

### 実行時の処理
プロジェクト直下（`c:\Users\subli\プログラミング\blog\tetsu-blog-reborn`）で以下のコマンドを実行する。

```bash
python fix_images.py
```
